const deployPath = "publishing/epubcheck"

module.exports = {
  "title": "EPUBCheck",
  "description": "The website of the official conformance checker for EPUB publications",
  url: `https://www.w3.org/${deployPath}`,
  pathPrefix: `/${deployPath}`
}
